'use strict';

/**
 * Car Specs Class
 */
function CarSpecs(name, armor, speed, power) {
    this.name = name;
    this.armor = armor;
    this.speed = speed;
    this.power = power;
};

module.exports = CarSpecs;